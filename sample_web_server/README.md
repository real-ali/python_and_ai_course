## Sample Web Server - FLASk ,render_html

### Installation:
You can install Flask using pip, the Python package manager:
`pip install Flask`

### run the project
You can run the project using this command:

`flask --app server --debug run`